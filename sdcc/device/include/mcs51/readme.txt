8051_orig.h, 8052_orig.h:

    Original header files from SDCC.

8051.h, 8052.h, compiler.h:

    Modified revision which can "work" well with the syntax analyzer of
    Eclipse CDT.

convert.py:

    A crude script to convert original header files using __sfr and __sbit
    to those using SFR() and SBIT().

